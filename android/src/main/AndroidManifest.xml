<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for background playback. Host apps must also declare these
         to actually use background mode; we declare them here so the plugin
         can bring the service on board without additional consumer changes
         when they enable the feature. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name=".MetronomeService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
